<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.ninng.dao.IStudentDao">

    <insert id="insertStudent" parameterType="top.ninng.domain.Student">
        <selectKey keyColumn="id" keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            select last_insert_id();
        </selectKey>
        insert into
        student(name, sex, address)
        values (#{name}, #{sex}, #{address})
    </insert>

    <update id="updateStudent" parameterType="top.ninng.domain.Student">
        update student
        set name=#{name},
            sex=#{sex},
            address=#{address}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete
        from student
        where id = #{id}
    </delete>

    <select id="selectAll" resultType="top.ninng.domain.Student">
        select *
        from student
    </select>

    <select id="selectByAddress" parameterType="java.lang.String" resultType="top.ninng.domain.Student">
        select *
        from student
        where address = #{address}
    </select>
</mapper>